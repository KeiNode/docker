mkdir -p ~/download && cd ~/download && git clone https://github.com/KeiNode/docker_no_redhat.git . && chmod +x install.sh uninstall.sh && sudo ./install.sh

^^^^^^^^
COPY THE COMMAND FOR INSTANT PROCESS!!!
